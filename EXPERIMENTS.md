# 实验记录与踩坑清单

> 本文档记录训练实验的完整演进、每次尝试的结论、以及踩过的坑（含原因与解法）。
> README 里的「关键经验」是精简摘要，这里是完整版。

## 分数演进

| 阶段 | 模型 | 训练尺度 | val mAP@50-95 | 提交分数 |
|------|------|----------|---------------|----------|
| baseline | yolov8n | 640 | 0.2168 | 32.989 |
| + 双流融合 | yolov8n | 640 | 0.2453 | 33.799 |
| + 双流融合 | yolov8s | 640 | 0.2562 | 36.426 |
| + 类别加权采样精修 | yolov8m | 640 | — | +2 分 |
| + 多尺度渐进 | yolov8m | 640→832 | — | +3.9 分 |
| + 多尺度渐进 | yolov8m | 640→1024 | 0.3730 | 46.523 |
| 基础训练 | yolov8l | 640 | 0.3635 | 待渐进后测 |
| 基础训练 | yolov8x | 640 | **0.3777** | 待渐进后测 |

**关键观察**：yolov8x 仅 640 尺度就 0.3777，已超过 yolov8m@1024 的 0.3730 —— 大模型是高分辨率之外的第二增长引擎。渐进到 1024 后预期显著更高。

## 踩坑清单

### 1. 输入分辨率「随机多尺度训练」让 DFL 崩（最严重）

- **现象**：`train_l_ms` 用 `train.multi_scale=[640,1024]` + 强仿射，val 从 0.184 崩到 **0.0205**。
- **根因**：YOLOv8 的 DFL 头框宽高是「网格单位」，COCO 预训练权重在 640 尺度标定。训练时随机切输入尺度（而 val 固定 1024），框宽高回归在训练/推理尺度间错乱。
- **解法**：放弃输入分辨率随机多尺度，改「**渐进训练**」——每步 +128（=+20%），DFL 通过小步回传逐步适应，快且稳。

### 2. 直接固定高尺度起步可行但慢 ~20%

- **实验**：直接 1024 训练 vs 640 起步，epoch8 val 0.0868 vs 0.1037。
- **根因**：预训练 DFL 是 640 标定，直接高尺度需回传重新校准 DFL，前期拖慢。
- **结论**：640 起步 + 渐进是最优路径，不要跳过。

### 3. 强增强在小数据集上拖垮收敛

- **现象**：scale ±50% + rotate ±10° 让 val 掉到 0.01（1600 训练样本太少）。
- **解法**：温和档 `scale ±25% + rotate ±5° + translate ±5% + 关剪切 + 关 mosaic`。既满足泛化又不破坏预训练特征。

### 4. 显存预算（24GB 卡）

| 配置 | 峰值显存 | 处置 |
|------|----------|------|
| yolov8l @1024 batch8 | 22.09 GB | 危险 → 降到 batch4 |
| yolov8x @640 batch8 | 11.08 GB | OK |
| yolov8x @1024 | — | 需 batch3（OOM 再降 2）|

### 5. 服务器操作坑（SSH/传输/定时/共享）

- **SSH 执行 `nohup` 会挂起不返回**，但进程实际已正确 detach。对策：用 Bash 工具后台运行 + 单独 `ps`/日志验证启动。
- **scp 管道 grep 返回假失败**（exit code 1）：`scp ... | grep -v ...` 当输出全被过滤时返回 grep 的 1。对策：`scp ... 2>/dev/null; echo "scp exit=$?"`。
- **CronCreate 定时任务不按时触发**：不依赖 cron，改手动检查。
- **共享服务器 GPU 被他人占满**：4 张卡曾被另一用户 `ndj` 的交通预测任务占满（37h+），需等待空卡，勿共卡（OOM + 拖慢对方）。

## 尝试记录（有效 / 无效）

### ✅ 有效

| 尝试 | 说明 |
|------|------|
| 双流融合 Stem | IR/Depth 残差分支，BN gamma 置 0 从零起步；比 5ch 直接拼接稳 |
| 温和仿射增强 | ±25% 缩放 + ±5° 旋转，泛化 + 不破坏收敛 |
| 渐进多尺度训练 | 640→768→896→1024，逐步解锁高分辨率推理，每步 +128 |
| 类别加权采样 | `1/sqrt(freq)` WeightedRandomSampler，弱类精修 +2 分 |
| 水平翻转 TTA | 推理阶段稳定提升 |
| 大模型 yolov8l/x | x@640 已超 m@1024，大模型杠杆明显 |
| 伪标签自训练 | 测试集高置信度框混入训练（1600→2600，覆盖测试分布）；待渐进后完整验证 |

### ❌ 无效 / 放弃

| 尝试 | 放弃原因 |
|------|----------|
| 输入分辨率随机多尺度 | DFL 崩（见坑 1）|
| 强增强（±50% 缩放 + ±10° 旋转）| 拖垮收敛（见坑 3）|
| 直接高尺度起步 | 慢 ~20%（见坑 2）|
| EMA | decay 0.9999 太慢，验证滞后 ~25 epoch，选中欠训练权重 → 关闭 |

## 当前流水线

1. 基础训练 640（yolov8l / yolov8x，温和仿射）→ 已完成
2. 渐进 768→896→1024（`scripts/run_multiscale_{l,x}.sh`，各占一卡并行）→ 待 GPU 空出
3. 伪标签重训（`scripts/pseudo_label.py` + `pseudo_train/`）→ 待
4. 多尺度 TTA 推理 + 提交
5. 待办：难类加重（focal loss 或 AP 加权采样），等 per-class AP 结果
